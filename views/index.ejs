<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.min.js"
      integrity="sha384-Atwg2Pkwv9vp0ygtn1JAojH0nYbwNJLPhwyoVbhoPwBhjQPR5VtM2+xf0Uwh9KtT"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
<!--     <div class="container">
      <form action="/getRoster" method="POST">
        <div class="row">
          <div class="col">
            <select name="team">
              <% teams.forEach(function (f) { %>
              '{"foo":"bar","one":"two"}'
              <option value="{teamID: <%= f.teamID %>, name: <%=f.name%>}">
                <%= f.abbreviation %> - <%= f.name %>
              </option>
              <% })%>
            </select>
          </div>
          <div class="col">
            <button type="submit" class="btn btn-primary">Go to Roster</button>
          </div>
        </div>
      </form>
    </div> -->

    <div class="container">
  <form action="/>" method="POST">
    <div class="dropdown">
      <button
        class="btn btn-secondary dropdown-toggle"
        type="submit"
        id="dropdownMenuButton1"
        data-bs-toggle="dropdown"
        aria-expanded="false"
      >
        Select a Team
      </button>
      <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
        <% teams.forEach(function (f) { %>
        <a class="dropdown-item" href="/team/<%= f.name %>"
          ><%= f.abbreviation %> - <%= f.name %></a
        >
        <% })%>
      </ul>
    </div>
  </form>
</div>

    <!-- Drop down to select a team -->
  </body>
</html>
